<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="com.ftinc.scoop">

    <application
        android:label="@string/app_name"
        android:supportsRtl="true">

        <!-- Flavor Settings Activity -->
        <activity android:name=".ui.ScoopSettingsActivity"
            android:theme="@style/Theme.AppCompat.Light"
            />

    </application>

</manifest>
